import carritoModel from "../models/cart.model";


export default class carrito{
    obtenerCarrito=()=>{
        try {
            return("todos los carritos obtenidos")
        } catch (error) {
            
        }
    }
}